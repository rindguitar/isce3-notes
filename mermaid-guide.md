# Mermaid 図の作成手順

このリポジトリに図を追加・更新するときの手順と原則。

別プロジェクトで図を作った際に**読みにくさを指摘されて 3 回作り直した**経験をもとにしている。
2026-08-27 にこのリポジトリ用へ書き直し、そのとき図を 8 枚作って得た知見を足した。

---

## このリポジトリでの前提

| 決めごと | 内容 |
|---|---|
| 形式 | ` ```mermaid ` フェンスで md に直接埋める。**GitHub が描画するので PNG はコミットしない** |
| 目視用の画像 | `/tmp` に出す。リポジトリに残さない |
| 言語 | **日本語で書いてよい**（`~/isce3-notes` の中なので）。`~/isce3` に出す図は英語 |

### 描く材料は実測に限る

**`~/isce3` のソースは自由に読める**（2026-08-28 に全面許可）。
依存関係を `grep` で実測してから描く、という元の手順書の前提が使えるようになった。

描く材料に使えるもの:

- **ソースの実測**（`grep` / ヘッダ構成 / runconfig の既定値）。いちばん強い
- Python から API を叩いた実測結果（`python - <<'PY'` で標準入力から実行）
- ディレクトリ一覧、git のメタ情報
- `gh api` で取った upstream の issue / PR

**推測で描くくらいなら描かない。** 間違った図は、無い図より害が大きい。
読めるようになった以上、**推測で描いたら手抜き**になる。

---

## 図の 3 原則

### 1. 矢印は、向きが情報のときだけ使う

| 図 | 矢印 | 理由 |
|---|---|---|
| リポジトリ配置（`README.md`） | **使う** | 「fork してクローンする」「ビルド先はここ」という向きが図の中身そのもの |
| 座標変換（wiki の `06 SAR / InSAR`） | **使う** | `rdr2geo` と `geo2rdr` は互いに逆向き。**向きが名前の意味そのもの** |
| 用語同士の関連を描く場合 | **使わない** | 「関連している」ことしか表さない。向きは表に書けば足りる |

矢印は情報を 1 つ増やすが、読む負荷も増やす。**その向きが無いと図が成立しないか**で判断する。

### 2. 略称を図に置かない

`RSLC` `GCOV` `ECEF` `LLH` `NEP 50` のような略称をそのまま置かない。
図は一瞬で読む前提なので、略称を頭の中で展開する手間が入るとそこで止まる。

```
悪い: DEM
良い: 地形<br/>数値標高モデル DEM
```

日本語を上、略称を下に添える。ラベルが 2 行になるが、そのほうが読める。

**ただし関数名は例外。** `rdr2geo` / `geo2rdr` は展開すると逆に分からなくなるうえ、
コードで実際に打つ名前なので、そのまま置く。

### 3. 1 枚の線を減らす

交差するかどうかは、ほぼ**1 枚あたりの線の本数**で決まる。実測では**1 枚 10 本前後**が上限。

このリポジトリの 9 枚は、いずれも **2〜6 本**に収めてある。

---

## 交差を減らす手順（この順で試す）

### 手順 1: まとめる（最優先）

密に繋がりあうものを 1 つの箱にまとめる。箱の中の線は図から消えるので、線が大きく減る。

**実例（wiki `rdr2geo / geo2rdr` の座標変換図）:**
最初は入力 5 個（方位時刻・斜距離・軌道・ドップラ・地形）をそれぞれソルバへ繋いで **5 本**だった。
「レーダ座標」「地図座標」の 2 つの箱にまとめ、**2 本**になった。

**まとめる方が、分けるより上位**である理由: 分割は全体像を失うが、まとめても全体像は残る。
「まとめた図（全体像）＋ 箱ごとの図（内訳）」の 2 階層にすれば、どちらも見られる。

### 手順 2: 循環を作らない

**今回いちばん効いた知見。**

`README.md` の図で、最初こう描いた。

```
手元のクローン → fork → 本家 → 手元のクローン
```

**この循環が交差の原因だった。** 循環は一列に並べる限り必ずどこかで戻り線が発生し、
その戻り線が他のノードや線を横切る。実際、「プルリクエスト」の矢印が図の上を大回りして
別の箱と点線を横切った。

対処は**循環を別の図に切り出すこと。**
`README.md` の図からは PR の流れを落とし、wiki `ブランチとプルリクエスト` の
「upstream 追従から PR まで」の図に任せた。結果、どちらも一直線になり交差が消えた。

### 手順 3: 分ける

まとめてもまだ多いなら、内訳をさらに分ける。分けるときは**番号を振り、見出しと対応させる**。
図に「手順 3」とあれば「### 手順 3」の節に飛べる、という状態にする。

### 手順 4: ほぼ全結合なら、図にしない

全ペアの 8 割以上が繋がっているまとまりは、描いても線が絡むだけで情報が伝わらない。
**一覧＋「繋がっていない組」の明記**に置き換える。

**実例（2026-08-28）:** ドキュメントマップの「ISCE3 の中身」は 4 ジャンル 5 本の図
だったが、ページを足した結果 **6 通り全部が繋がって全結合になった**。
この規則に従って図を消し、ジャンル間のリンク本数を表にした。
全結合の図は「どこが繋がっているか」を何も語らない。

### 図から落とした線は、必ず本文で補う

分けたりまとめたりすると、描かれない線が出る。**その線を表や文章で残す。**
図を読みやすくするために情報を捨ててはいけない。

座標変換図で落とした「3 つの拘束」の内訳は、
wiki `rdr2geo / geo2rdr` の表に残してある。図と表がセットで初めて成立する。

---

## 必ずやること: 画像に描き出して目で見る

**構文チェックが通ることと、読めることは別物。**
`mermaid.parse()` が通っても、線がノードを貫通していれば誤読される。

### 準備（初回のみ）

```bash
# 描画ツール
npm install -g @mermaid-js/mermaid-cli

# 日本語フォント（WSL なら Windows 側から借りる。Meiryo が入っていれば描画できる）
fc-list :lang=ja family | sort -u
```

WSL / Docker ではサンドボックスを切る必要がある。設定ファイルを 1 つ作っておく。

```bash
echo '{"args":["--no-sandbox","--disable-setuid-sandbox","--disable-dev-shm-usage"]}' > /tmp/pconf.json
```

### 描き出して見る

```bash
mmdc -i fig.mmd -o fig.png -p /tmp/pconf.json -b white -s 2
```

生成された PNG を開いて、次の **3 つ**を確認する。

| 見るもの | 何が起きているか |
|---|---|
| **交差** | 線と線が交わっていないか |
| **貫通** | 線がノードの箱の上を通っていないか（交差より誤読を招く） |
| **並び順** | 左から右／上から下に読んで、意味の順序と一致しているか |

### 並び順を変えると結果が変わる

Mermaid は**宣言する順序でレイアウトが変わる。**

**実例（座標変換図）:** 「レーダ座標」を先に宣言したのに、描くと**右**に置かれた。
`rdr2geo`（レーダ → 地図）の矢印が右から左を向き、**向きは正しいのに読み順が逆**になった。
`subgraph` の宣言順を入れ替えたら期待どおりになった。

**実例（ドキュメントマップの「ISCE3 の中身」、2026-08-28）:** 4 ジャンルが三角形 2 つで
繋がる形になり、線が 1 箇所で交差した。**上下方向の辺を先に、横に渡る辺を最後に宣言**
し直したら交差が消えた。ノードも辺も変えていない。宣言順だけの違い。

交差や逆順が出たら、順序を入れ替えた版を 2〜3 個作って描き比べ、いちばん綺麗なものを採用する。
**描いて見比べるのが確実。**

### 表示と構造の食い違いを疑う

ラベルに書いたことと、線の張り方が食い違っていないか。

**実例（学問分野の図）:** 箱に「土台（**どの段でも要る**）」と書いたのに、
矢印は「見た点はどこか」の 1 箇所にしか伸びていなかった。
全段に繋ぐと線が増えるので、**ラベルを「幾何を解くのに要る土台」に直して実態に合わせた。**

図を読みやすくするためにラベルを盛ると、こういうズレが生まれる。

---

## このリポジトリの図の一覧

| 図 | 置き場所 | 何を表すか | 線 |
|---|---|---|---|
| 3 つのパスの関係 | [README.md](README.md) | `~/isce3` `~/isce3-build` `~/isce3-notes` の役割と、本家との関係 | 3 |
| 座標変換 | [rdr2geo / geo2rdr](https://github.com/rindguitar/isce3-notes/wiki/Rdr2geo-Geo2rdr) | `rdr2geo` と `geo2rdr` が繋ぐ 2 つの座標系 | 2 |
| 処理の段と学問分野 | [学問分野](https://github.com/rindguitar/isce3-notes/wiki/Disciplines) | どの工程がどの分野に対応するか | 6 |
| ビルドからテストまで | [ビルドの全体像](https://github.com/rindguitar/isce3-notes/wiki/Build-Overview) | ソースから `import isce3` までの流れ | 6 |
| 貢献の流れ | [ブランチとプルリクエスト](https://github.com/rindguitar/isce3-notes/wiki/Pull-Request) | upstream 追従から PR・レビュー待ちまで | 5 |
| 開発環境のジャンル相関 | [ドキュメントマップ](https://github.com/rindguitar/isce3-notes/wiki/Documentation-Map) | 環境 → ビルド → テスト → git の一本道 | 5 |
| NISAR データの取得 | [NISAR Data Access](https://github.com/rindguitar/isce3-notes/wiki/NISAR-Data-Access) | 認証の関門（アカウント→認可→トークン）と、検索は認証不要であること | 4 |
| 処理レベルの連鎖 | [処理レベルの連鎖](https://github.com/rindguitar/isce3-notes/wiki/Processing-Chain) | 生データから各プロダクトへの流れと、担当するワークフロー | 4 |
| 答え合わせの仕組み | [処理レベルの連鎖](https://github.com/rindguitar/isce3-notes/wiki/Processing-Chain) | 自分が作った出力と公式の出力を突き合わせてバグを見つける道筋 | 4 |

**図を足したらこの表も更新する。** どこに何があるか分からなくなると、
同じ図を 2 箇所に置いたり、内容がずれたまま放置されたりする。

---

## 図を追加・更新する手順

1. **描く材料を確保する。** ソースを読んで裏を取る。**推測で描かない**
2. 上の 3 原則に沿って下書きする。`/tmp` に `.mmd` として置く
3. 交差を減らす手順（まとめる → 循環を作らない → 分ける）を適用する
4. **画像に描き出して、交差・貫通・並び順を目で見る**
5. 直したら描き直して、また見る。順序違いの版を描き比べる
6. ` ```mermaid ` フェンスとして対象の md に埋め込む
7. 上の一覧表を更新する
8. コミットする

---

## 出す前のチェックリスト

- [ ] 矢印を使っているなら、その向きは図に必要か
- [ ] 略称を単独で置いていないか（関数名は例外）
- [ ] 1 枚の線は 10 本以下か
- [ ] **循環していないか**（循環は必ず交差する。別の図に切り出す）
- [ ] 図から落とした線を、表や本文で補ったか
- [ ] **ラベルに書いたことと、線の張り方が食い違っていないか**
- [ ] 番号と見出しが対応しているか
- [ ] **全部の図を画像に描き出して、交差・貫通・並び順を目で見たか**
- [ ] ラベルが途中で切れて意味不明になっていないか
- [ ] 一覧表を更新したか
- [ ] PNG をコミットしていないか
