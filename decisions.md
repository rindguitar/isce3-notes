# 設計判断の記録

コードを書く前後に下した判断を、理由ごと残す。**新しいものを上に追記する。**

記入形式:

```markdown
## YYYY-MM-DD: （決定のタイトル）
- 決定: （何を選んだか）
- 理由: （なぜか。1〜2行）
- 却下した代替案: （あれば）
```

---

## 2026-08-27: 用語集を wiki へ移す

- 決定: `glossary/` を丸ごと GitHub wiki へ移し、リポジトリ本体からは削除する。
  `README.md` と `CLAUDE.md` の「用語集に追記する」規定も wiki 宛に書き換えた
- 理由: 用語集は「読んで学ぶもの」で、wiki のほうがサイドバー付きで読みやすい。
  リポジトリ本体は作業の記録（ログ・現況・判断）に絞る
- 却下した代替案:
  - 本体に残す — セッション中に読み書きしやすいが、閲覧性は wiki に劣る
  - 本体を正として wiki へ複製 — 同期スクリプトが要り、二重管理になる
- ⚠️ **副作用: wiki は別リポジトリなので、Claude Code のセッションから直接は読み書きできない。**
  用語集に追記するには `git clone https://github.com/rindguitar/isce3-notes.wiki.git` が要る。
  本体からの相対リンクも `@import` も効かない
- 補足: 移すときに章どうしのリンクを実測したところ、**02→03 と 06↔08 の 3 本しかなかった**。
  01（言語）と 04（git）は他章から一度も参照されていない。
  用語集は思ったより繋がっておらず、独立した 3 つの塊に分かれていた（→ wiki の索引ページの図）

## 2026-08-27: 貢献の主軸は issue 起票に置き、PR はマージ待ちの前提で出す

- 決定: upstream への貢献は「**issue を立てる / 既存 issue に再現データを足す**」を主軸とし、
  PR は出すが**マージまで数か月かかる前提**で扱う
- 理由: 実態を数えた結果、**PR は作者が誰かに関係なく滞留している**。
  open PR 65 件、最古は 2024-11 の #27（約 21 か月）。コア開発者 gshiroma だけで 17 件が open。
  外部の方が出した #270 は 3 か月レビューされていない（review_comments = 0）。
  ボトルネックは「外部かどうか」ではなく**レビュー能力**
- 却下した代替案:
  - PR を主軸に据える — マージされないことを自分の失敗と誤認しやすい。動機が保たない
  - 貢献自体を諦める — issue とコメントには反応があるので、そこは機能している
- 補足: ユーザーは ISCE3 の専門分野が未学習。**issue 起票なら専門知識の不足を
  「環境の違いによる再現」で補える**（手元は GDAL 3.13.3 / HDF5 2.2.0 / GCC 15.3 と
  upstream が試していない組み合わせ）

## 2026-08-27: 試行コードはファイルに残さず標準入力から実行する

- 決定: 探索・学習目的のコードは `.py` ファイルにせず、
  `python - <<'PY' ... PY` のヒアドキュメントで実行する
- 理由: 使い捨てのコードでリポジトリを汚さないため。
  加えて**段階ごとに分けて流せる**ので、出力を都度解釈しながら進められる
- 却下した代替案:
  - `scratch/` ディレクトリを作って置く — `.gitignore` の管理が要るうえ、
    残しても後で読み返さない
- 補足: ファイルとして残すのは、再利用が前提のツールになったときだけ。
  **成果として残したい内容は `logs/` や wiki の用語集に散文で書く**

## 2026-08-27: 最初の題材として幾何（rdr2geo / geo2rdr）を選んだ

- 決定: ISCE3 の理解は `core` / `geometry` を外から叩くところから始める。
  テストを読む案・ワークフローを通す案は見送った
- 理由: **`~/isce3` のソース読み取り許可が不要**で、データ準備もいらず即座に動かせる。
  かつ rdr2geo / geo2rdr は上位処理すべてが乗る土台なので、
  ここが分かると他の領域の見通しが立つ
- 却下した代替案:
  - `tests/python/` を実例として読む — 実際の入力データの形が見えるが、読み取り許可が要る
  - 同梱データでワークフローを 1 本通す — 一番「動かした」感があるが、
    データ量と実行時間が読めず、`tests/data/` と `share/` の許可も要る
- 補足: 副産物として、未解決の `GeoToRdr` に対して
  **「正常系は通る」ことが分かった**。追うなら失敗する入力条件の絞り込みになる

## 2026-08-23: `CLAUDE.md` は `~/isce3-notes` に置き、`--add-dir` で運用する

- 決定: `~/isce3-notes/CLAUDE.md` を作り、`cd ~/isce3-notes && claude --add-dir ~/isce3` で起動する
- 理由: Claude Code は起動時のカレントディレクトリから親へ遡って `CLAUDE.md` を探すので、
  `~/isce3` に置く必要がない。**`~/isce3` を完全に無傷に保てる**
- 却下した代替案:
  - `~/isce3` にシンボリックリンク + `.git/info/exclude` — git は汚れないが、
    ディレクトリにエントリが増えることに変わりはない
  - `~/.claude/CLAUDE.md`（ユーザーレベル）— 他の全プロジェクトにも効いてしまう

## 2026-08-23: `GeoToRdr` の失敗は未解決のまま開発を進める

- 決定: 原因（最適化起因、UB の疑い）まで特定した上で、修正はせず既知の失敗として運用する
- 理由: 237 件中 1 件であり、開発を始める支障にならない。
  **基準値 235/237 を把握していれば、自分の変更による失敗と区別できる**
- 却下した代替案:
  - GCC を 15 未満に降格 — フル再ビルドのコストに対し、得られるものが「1 件通る」だけ
  - Debug ビルドを常用 — 実行が遅くなる

## 2026-08-23: 依存は手動で上限を指定する（spec file には乗り換えない）

- 決定: `eigen<4` と `pybind11<3` を手動で固定する
- 理由: 必要最小限の変更で済み、何をなぜ固定したかが明示的に残る
- 却下した代替案:
  - ISCE3 の pinned な conda spec file に乗り換える — 根本的だが、実体が `tools/` 配下で未確認。
    **次に依存起因で詰まったときの手札として保留**（`reference/runtime-and-packaging.md`）

## 2026-08-23: CUDA ビルドは行わない

- 決定: 常に `-DWITH_CUDA=OFF` を明示する
- 理由: システムの nvcc は CUDA 12.0 で対応ホスト GCC が 12.x まで、環境の GCC は 15.3 で範囲外。
  かつ `environment.yml` に CUDA が含まれておらず、プロジェクトが想定する構成ではない
- 却下した代替案:
  - conda-forge の `cuda-nvcc` を入れて揃える — 可能だが、まず CPU 版で通すことを優先した。
    **GPU が必要になった時点で再検討する**
- 補足: `WITH_CUDA` の既定は `Auto` で、**nvcc を見つけると勝手に ON になる**。
  明示的に OFF を指定しないと意図せず CUDA ビルドに入る

## 2026-08-23: メモは fork の wiki ではなく独立したリポジトリに置く

- 決定: `~/isce3-notes`（github.com/rindguitar/isce3-notes）を新規に作る
- 理由: **fork を削除すると wiki も一緒に消える。** fork は PR マージ後や
  取り直しのときに実際に消すことがあり、「いつでも見れる記録」という目的と衝突する
- 却下した代替案:
  - fork の GitHub wiki — 上記のとおり消えるリスク。また wiki は本体と別リポジトリで、
    コード検索にも引っかからない
  - fork のコードツリーにメモを置く — upstream との差分に混ざり、PR の邪魔になる
- 補足: ライセンス（Apache 2.0）・輸出管理（EAR99 NLR = 許可不要）の両面で公開に制約はない
- 2026-08-27 追記: **公開リポジトリにしてある。** wiki を使うための必然で、上記のとおり制約もない

## 2026-08-23: ビルドディレクトリをリポジトリの外に置く

- 決定: `~/isce3-build`（`cmake -S ~/isce3 -B ~/isce3-build`）
- 理由: `~/isce3` に生成物を一切作らない。**`.gitignore` を編集する必要もなくなる**
- 却下した代替案:
  - `~/isce3/build/` — 慣例的だが、リポジトリ内にファイルが増える

## 2026-08-23: ビルドは CMake 直叩きにする（`pip install .` から移行）

- 決定: `cmake -S ~/isce3 -B ~/isce3-build -G Ninja -DISCE3_FETCH_DEPS=OFF -DWITH_CUDA=OFF`
- 理由: **`pip install .` では `ctest` が使えない**（ビルドツリーが残らない）。
  CONTRIBUTING.md は PR 前のローカルテストを要求している。加えて差分ビルドが効かず、
  1 行変えるたびにフルビルド相当になり開発サイクルが成立しない
- 却下した代替案:
  - `pip install .` のまま — 公式推奨だが「使うだけ」向け。リポジトリを汚さない利点はある
  - `pip install -e .`（editable）— C++ の変更には結局再ビルドが必要で、解決にならない
- 補足: 直叩きでは `ISCE3_FETCH_DEPS` の既定が **`ON`** になり、Eigen 等をネットから
  取り直して conda のものを無視する。**`OFF` の明示が必須**
